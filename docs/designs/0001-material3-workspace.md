# 登录后 Material 3 工作区

状态：实施中。日期：2026-09-03。

## 页面与视觉约定

保留登录注册行为与品牌紫色，统一深浅色、文字层级、圆角、48dp 触控目标和安全区域。四个主入口为概览、行动、记忆、洞察；600dp 以下使用底部导航，宽屏使用侧边导航。设置从顶部账户图标进入。列表阅读优先，详情按需编辑，危险操作不出现在首页。

- 概览：最近三条洞察、最近三个待处理提交、分析截图入口；无数据时使用真实引导。
- 行动：待处理/全部记录，按提交归组，分页、刷新、历史详情。详情先展示行动摘要，转录与依据展开查看。
- 新建分析：图片预览、补充说明、云端处理说明、明确上传/取消；系统分享仅预填，不自动上传。
- 行动详情：保存修改、设备预检、确认版本、授权写入依次进行；使用中文字段、日期时间选择器和设备账户选择器。回执待同步时不能重复执行。
- 记忆：阅读摘要、搜索与类型/时间筛选，高级筛选保留联系人标识；详情查看来源、修订、删除。
- 洞察：结论、事实/推断、依据、下一步建议、来源跳转；不把分析摘要伪装为正式洞察。
- 设置：密码、导出、退出；清空记忆与删除账户单独分组并确认。

## 状态与恢复

显示等待上传、排队分析、分析中、完成/失败的真实阶段，无虚构进度和预计时间。网络异常可重试同步，但不能生成新提交。后台 WorkManager 继续执行，前台恢复主动刷新。各页面独立加载与报错。保存导航、列表位置、筛选与编辑草稿，未保存离开需确认。账号切换清空旧界面状态并拒绝旧请求结果。

Room v3→v4 将 localImagePath 改为可空并保留所有既有数据。仅 pending_upload 的本地记录可以上传；远端恢复记录没有图片路径，不下载原图。云端列表与本地缓存以 accountId/id 合并，本机待同步回执优先。来自其他设备的已确认/执行中卡片没有本机预检/执行记录时只读，不移植设备授权。

## 历史接口

GET /v1/submissions?view=all|attention&limit=20&cursor=…，返回 {items,nextCursor}。limit 为 1–50，默认 20；cursor 是 base64url 编码的 {createdAt,id}，非法值返回 400。按 createdAt DESC,id DESC 排序并使用键集分页。

items 仅含 id、createdAt、updatedAt、source、status、actionCounts（各状态数量），不含截图地址、正文、补充文本或模型信息。attention 包含 uploaded/processing/failed 提交，或有 draft/blocked/ready/confirmed/executing 卡片的提交。已删除记录不返回。详情复用现有接口。

所有查询通过 verified JWT → withAccount → 强制 RLS，并显式限定账户；不新增公共权限、不修改云端业务表。每页批量查询卡片数量，不逐条抓取详情。

## 验证与提交

依次提交历史接口、导航主题、截图与行动、概览记忆洞察设置、验收修正。测试覆盖分页/隔离、Room 迁移、远端恢复不上传、状态恢复、确认与回执安全、深浅色/窄屏/横屏/200% 字体/输入法。保留 emulator-5554 的现有登录与数据，不卸载；使用合成截图与明确测试目标，不修改真实联系人或日历。
